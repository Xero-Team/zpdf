# Normalize line endings to LF in the repository and on checkout, so rustfmt
# and CI behave identically across Linux/Windows/macOS runners.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.ttf binary
*.otf binary
*.cff binary
